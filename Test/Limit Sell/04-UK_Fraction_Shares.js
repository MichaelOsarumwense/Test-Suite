describe('Limit Sell Collection', () => {
    beforeEach(() => {
        // Assuming Appium driver and capabilities are already set up
        // To be replace with actual app navigation code
        driver.startActivity('your.app.package', 'YourPortfolioActivity');
        // Navigate to the portfolio landing page
    });
    it('should prevent selling fraction of a UK stock', () => {
        // Steps for Limit Sell (UK Shares Failed Attempt to Sell Fraction of a Share)
        // Example: Click on desired portfolio, select sell, try to enter fractional details, and verify error message
    });


});
